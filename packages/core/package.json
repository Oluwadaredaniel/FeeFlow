{
  "name": "@feeflow/core",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-agnostic reconciliation engine, domain types, money utilities, and Nomba webhook verification for FeeFlow.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "tsx --test src/reconciliation.test.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4"
  }
}
